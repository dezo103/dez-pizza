import React from 'react';

type ReturnProps = {
  className?: string;
};

export const UseFilterIngredients: React.FC<ReturnProps> = ({ className }) => {
  return <div className={className}>there will be body of hooks</div>;
};
